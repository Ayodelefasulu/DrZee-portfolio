import type { Metadata } from "next";
import "./globals.css";
import SiteHeader from "@/components/SiteHeader";
import SiteFooter from "@/components/SiteFooter";

export const metadata: Metadata = {
  title: "Dr. Zaharatu Mohammed Babika \u2014 Environmental Scientist & Groundwater Researcher",
  description:
    "Chief Environmental Scientist at NOSDRA and PhD groundwater engineer (University of Bath). Research profile, publications and CV.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <SiteHeader />
        <main>{children}</main>
        <SiteFooter />
      </body>
    </html>
  );
}
